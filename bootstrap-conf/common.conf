# CudaText: lexer_file="Bash script"; tab_size=2; tab_spaces=yes;

dist=alma
distro=${dist}linux
DisTro=AlmaLinux
baseroot=BaseOS
os_packages=os/Packages

arch=$(uname -m)

echo arch=$arch

sudo=sudo
rpms_dest=.rpms

pkg0="microdnf"
pkg1="dnf langpacks-en"

package_mirror=https://mirror.dal.nexril.net
